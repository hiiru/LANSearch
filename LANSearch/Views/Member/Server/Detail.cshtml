@inherits Nancy.ViewEngines.Razor.NancyRazorViewBase<LANSearch.Models.Server.ServerDetailModel>
@{
    Layout = "Master/Master";
    ViewBag.Title = "Member .:. Server .:. Details";
}
<div class="col-xs-12 page-server">
    <div class="row">
        <h2 class="col-sm-6 media-heading">Manage Server</h2>
        <form method="POST" action="@Url.Content(string.Format("~/Member/Server/Detail/{0}/Action",Model.Server.Id))">
            <div class="btn-toolbar pull-right" role="toolbar">
                <div class="btn-group"><button type="submit" name="delete" value="true" class="btn btn-danger confirm-button" data-confirm="Are you sure you want to delete this Notification?"><span class="glyphicon glyphicon-remove"></span> Delete Server</button></div>
            </div>
            @Html.AntiForgeryToken()
        </form>
    </div>
    <hr />
    @Html.Partial("Partials/Server/DetailSummary.cshtml", Model)
    <hr />
    @Html.Partial("Partials/Server/DetailForm.cshtml", Model)
</div>