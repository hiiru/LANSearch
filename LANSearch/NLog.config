<?xml version="1.0" encoding="utf-8" ?>
<nlog xmlns="http://www.nlog-project.org/schemas/NLog.xsd" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
  <extensions>
    <add assembly="NLog.Targets.Redis" />
  </extensions>
  <targets>
    <target xsi:type="File" name="logfile" fileName="lansearch.log" />
    <target xsi:type="File" name="logfile-request" fileName="requests.log" />
    <target xsi:type="Console" name="console" />
    <target xsi:type="Redis" name="redis" host="127.0.0.1" key="logstash" dataType="list" />

    <target name="mRequests" xsi:type="MethodCall" className="LANSearch.Hubs.TestHub, LANSearch" methodName="LogRequest">
      <parameter layout="${date}" />
      <parameter layout="${message}" />
    </target>

    <target name="mLog" xsi:type="MethodCall" className="LANSearch.Hubs.TestHub, LANSearch" methodName="Log">
      <parameter layout="${date}" />
      <parameter layout="${level}" />
      <parameter layout="${callsite}" />
      <parameter layout="${message}" />
    </target>
  </targets>
  <rules>
    <logger name="LANSearch.LoggerMiddleware*" minlevel="Info" writeTo="logfile-request" />
    <logger name="LANSearch.LoggerMiddleware*" minlevel="Info" writeTo="mRequests" final="true" />
    <logger name="*" minlevel="Info" writeTo="redis" />
    <logger name="*" minlevel="Info" writeTo="logfile" />
    <logger name="*" minlevel="Info" writeTo="console" />
    <logger name="*" minlevel="Info" writeTo="mLog" />
  </rules>
</nlog>